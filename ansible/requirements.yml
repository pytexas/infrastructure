# ABOUTME: Ansible Galaxy collections required by the playbook.
# Install with `ansible-galaxy collection install -r requirements.yml`.

collections:
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=8.0.0"
  - name: community.docker
    version: ">=3.10.0"
  - name: community.sops
    version: ">=1.6.0"
